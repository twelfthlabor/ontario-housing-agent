# Ontario Housing Atlas frontend

The app opens to an interactive Ontario map and a searchable city index.
The former chat-dashboard layout has been replaced. Design references,
research on X, access limitations, and geographic provenance are documented in
[FRONTEND-RESEARCH.md](FRONTEND-RESEARCH.md).

## Main flows

- Search cities with the search box or `/` shortcut. Sort by featured order,
  city name, lowest median, or highest median.
- Expand “Median price ceiling” to filter cities by their median asking price.
  This does not filter individual listings.
- Select a city from the map or index. Its detail tray shows the exact sample
  median, listing count, share under $1m, and bedroom medians.
- Drag the map, use the zoom buttons, scroll to zoom, or fit Southern Ontario /
  all sampled cities. Close markers are decluttered; every city remains
  available in the index.
- Compare two cities using the workspace tab or a city's Compare control.
  Bedroom bars use a shared zero baseline across both cities, with exact prices.
- “Ask about this city” / “Ask about this comparison” opens the agent panel and
  fills a reviewable question. Enter sends; Shift+Enter adds a newline.
- The agent panel keeps conversation state when closed. New chat clears it.
  Dialogs trap focus and close with Escape. Reduced motion removes transitions.
- On a phone, use the Map / Cities switch. The comparison and agent flows remain
  available without a long stack of desktop panels.

The atlas uses the existing generated housing summary. The research agent,
API, guardrails, and listing dataset are unchanged. The offline demo is labeled
inside the agent panel and dataset information.

## Implementation

- `components/Workspace.tsx`: application state, explorer, detail tray,
  comparison, and modal entry points.
- `components/MapCanvas.tsx`: offline SVG basemap, marker placement, map camera,
  pan, zoom, and fit.
- `components/Chat.tsx`: existing SSE transport with the redesigned agent UI.
- `public/geography/ontario.json`: 36 GeoNames reference points and Natural Earth
  provinces / lakes, generated by `scripts/build_geography.py`.
- `public/fonts/`: self-hosted fonts and license files.

No new JavaScript dependency or map service account is required.

## Preview

```bash
npm run build
MOCK_LLM=1 npm run start -- --hostname 127.0.0.1 --port 3200
```

Screenshots are in `output/playwright/atlas-*.png`. Live-provider answer quality
is still a separate milestone from frontend verification.

## Verification on September 14, 2026

- Production build and TypeScript checking passed; all 61 existing Vitest tests
  passed.
- Browser checks covered search, selection, no-results recovery, city-median
  filtering, sorting against source data, map-marker selection, northern-city
  fit, zoom buttons, drag pan, and scroll zoom.
- Hamilton / Kitchener comparison displayed $749,900 / $605,000 and the correct
  $144,900 difference. Its prefilled agent question returned the corresponding
  offline SSE answer.
- Mobile checks covered city selection returning to the map, dataset info,
  Escape dismissal, and switching from the index into the comparison.
- Widths 320, 390, 600, 768, 1024, and 1440 had no page overflow. Reduced-motion
  settings disabled chart transitions.
- The first sort assertion incorrectly expected Thunder Bay to have the lowest
  median. Source-data inspection showed Sault Ste. Marie was lower; the UI's
  ordering was already correct and the assertion was corrected.
- Fresh production loads at 320×568 and 390×844 now explicitly verify that the
  selected map point stays above the detail tray. Initial mobile camera framing
  and label clearances were corrected after visual inspection. The final build
  passed, and the production page loaded without console errors.
